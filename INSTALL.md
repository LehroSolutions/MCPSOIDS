# Install (Bun)

From this repository root:

```bash
bun install
bun run build
bun run start
```

The JS package lives in `mcp_server_ref/`. A root workspace `package.json` is provided so `bun install` works at the repo root.

Equivalent nested install:

```bash
cd mcp_server_ref
bun install
```
